{
  "name": "atlas",
  "version": "0.1.0",
  "private": true,
  "description": "Terminal orchestrator for humans and AI agents",
  "author": "Arex95",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Arex95/atlas.git"
  },
  "packageManager": "pnpm@10.24.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.24.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo 'Run scripts/dev.sh to launch web (3000) + atlas-server (4000) together' && exit 0",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "typecheck": "pnpm --filter web exec vue-tsc --noEmit && pnpm --filter @atlas/domain typecheck",
    "format": "prettier --write \"**/*.{ts,vue,json,md}\""
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.1.3",
    "typescript": "^5.5.4"
  }
}
