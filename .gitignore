# Rust build output — regenerated by cargo, never committed.
/target/

# Local environment overrides. `.env` in the repo would ship secrets by
# accident; the sample `.env.example` is what belongs here instead.
.env
.env.local
.env.*.local

# Editor / OS scratch.
.idea/
.vscode/
.DS_Store
Thumbs.db
*.swp
*~

# Local Atlas data if the binary is run from the repo root.
/atlas-data/

# Documentation site
docs/node_modules/
docs/.vitepress/dist/
docs/.vitepress/cache/
